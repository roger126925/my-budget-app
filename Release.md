# Release Notes — 記帳本

個人記帳 Web App，支援信用卡分期、共同帳戶、預算追蹤。

---

## 功能總覽

### 帳戶管理
- 支援多帳戶（現金、銀行帳戶、信用卡）
- 每個帳戶有名稱、顏色、餘額
- 可設定為「共同帳戶」供家庭成員共用

#### 信用卡帳戶
- 類型設為「信用卡」後，餘額代表**待繳金額**（欠款）
- 支出 → 待繳增加；收入/退款 → 待繳減少
- 轉帳到信用卡 → 視為繳卡費（待繳減少）
- 帳戶卡顯示橘紅色「卡」標籤 + 「待繳 $X」

#### 出帳日
- 每張信用卡可設定每月出帳日（例：15 號）
- 建立分期時，系統依刷卡日自動判斷首期落在哪個月：
  - 刷卡日 ≤ 出帳日 → 本月帳單
  - 刷卡日 > 出帳日 → 下月帳單

---

### 記帳

#### 一般收支
- 選帳戶、分類、金額、日期、備註
- 新增 / 編輯 / 刪除，帳戶餘額自動更新

#### 轉帳
- 在帳戶間轉移金額
- 來源帳戶餘額減少，目標帳戶餘額增加
- 轉入信用卡視為繳費（待繳減少，而非增加）

#### 信用卡分期
選擇信用卡帳戶後，記帳表單會出現「單筆 / 分期」切換：

**單筆**：照一般流程記帳。

**分期**：
1. 填入總金額與期數
2. 系統即時顯示每月金額與首期出帳月份
3. 按「建立分期」→ 寫入分期計畫，不建立交易
4. 之後每月到設定頁「分期管理」點「本月繳款」記帳

---

### 分期管理（設定頁）
顯示所有進行中的分期計畫：
- 名稱、使用帳戶、每月金額
- 進度條：已繳 / 剩餘期數、剩餘總額
- 「本月繳款」按鈕 → 自動建立該月支出交易並更新信用卡待繳金額
- 已繳清的分期顯示綠色完成狀態
- 可刪除分期紀錄

---

### 預算設定（設定頁）
- 每月各支出分類設定預算上限
- 進度條顯示當月使用比例：
  - 綠色：< 80%
  - 橘色：80–100%
  - 紅色：超出預算

---

### 共同帳戶（設定頁）
多人共用帳戶，適合家庭或情侶記帳：

1. 建立群組（自動產生 6 碼邀請碼）
2. 另一方輸入邀請碼加入
3. 帳戶管理勾選「設為共同帳戶」，雙方都能看到並記帳
4. 共同帳戶卡片顯示藍色「共」標籤

摘要區塊顯示：
- 月預算（可設定）
- 本月已花 / 剩餘預算
- 共同帳戶總餘額
- 進度條

---

### 月份篩選與統計
- 左右箭頭切換月份，僅顯示該月交易
- 圓餅圖：各分類支出比例（純 SVG，無外部套件）
- 可依分類或帳戶過濾交易列表

---

### 匯出 CSV
- 依目前篩選條件匯出
- 含 BOM（讓 Excel 正確顯示中文）
- 欄位：日期、類型、帳戶、分類、金額、備註

---

### 資料管理（設定頁）
- 選月份檢視交易列表
- 單筆刪除：自動反向修正帳戶餘額
- 清除全部交易（需二次確認）
- 帳戶編輯可直接修正錯誤餘額

---

## 邏輯說明

### 餘額更新邏輯

| 操作 | 一般帳戶 | 信用卡帳戶 |
|------|----------|------------|
| 新增支出 | 餘額 − 金額 | 待繳 + 金額 |
| 新增收入 | 餘額 + 金額 | 待繳 − 金額 |
| 刪除支出 | 餘額 + 金額（還原） | 待繳 − 金額（還原） |
| 刪除收入 | 餘額 − 金額（還原） | 待繳 + 金額（還原） |
| 轉帳轉出 | 餘額 − 金額 | 餘額 − 金額 |
| 轉帳轉入 | 餘額 + 金額 | 待繳 − 金額（繳費） |

### 分期首期計算邏輯

```
刷卡日（day）≤ 出帳日（billing_day）→ start_month = 本月
刷卡日（day）> 出帳日（billing_day）→ start_month = 下月
未設出帳日 → start_month = 本月（預設）
```

範例（出帳日 15 號）：
- 6/10 刷卡 → 6/15 出帳 → 首期 **2026-06**
- 6/20 刷卡 → 7/15 出帳 → 首期 **2026-07**

### 分期進度計算邏輯

```
elapsed = (今年 − 首期年) × 12 + (今月 − 首期月)
已繳期數 = clamp(elapsed, 0, 總期數)
剩餘期數 = 總期數 − 已繳期數
剩餘金額 = 剩餘期數 × 每月金額
```

### 資料隔離（RLS）

- `accounts`、`transactions`、`categories`、`budgets`：`user_id = auth.uid()`，各用戶只看自己的資料
- 共同帳戶（`household_id` 不為 null）：透過 `get_my_household_id()` SECURITY DEFINER function 判斷成員資格，群組成員皆可讀寫
- `household_members`：只能看到自己的成員記錄

---

## 部署資訊

- 平台：Vercel（免費）
- Repo：https://github.com/roger126925/my-budget-app.git
- DB：Supabase（PostgreSQL + Auth + RLS）
- 注意：Supabase 免費方案閒置一週自動暫停，需手動 Restore
